/*
因为默认值20.0的类型为Celsius类型,而该类型又定义了String方法，也就是定义了默认的输出格式，会自动在数字后面加上°C
*/
package main

import "fmt"

func main() {
	fmt.Println("因为默认值20.0的类型为Celsius类型,而该类型又定义了String方法，也就是定义了默认的输出格式，会自动在数字后面加上°C")
}
